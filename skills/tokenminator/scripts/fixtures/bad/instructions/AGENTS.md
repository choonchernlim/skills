# Fixture

See `src/missing/` for the code.

## Notes

- Filler guidance line 0 that adds nothing an agent needs.
- Filler guidance line 1 that adds nothing an agent needs.
- Filler guidance line 2 that adds nothing an agent needs.
- Filler guidance line 3 that adds nothing an agent needs.
- Filler guidance line 4 that adds nothing an agent needs.
- Filler guidance line 5 that adds nothing an agent needs.
- Filler guidance line 6 that adds nothing an agent needs.
- Filler guidance line 7 that adds nothing an agent needs.
- Filler guidance line 8 that adds nothing an agent needs.
- Filler guidance line 9 that adds nothing an agent needs.
- Filler guidance line 10 that adds nothing an agent needs.
- Filler guidance line 11 that adds nothing an agent needs.
